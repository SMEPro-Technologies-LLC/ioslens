# ioslens
IOS+ operates as the **Model Context Protocol (MCP) server**. This is a control-plane role, not a conversational role. The MCP server does not chat with users. It publishes tools, resources, and structured outputs to MCP clients. It enforces policy. It resolves crosswalks. It executes gates. It logs decisions. It returns verdicts.
